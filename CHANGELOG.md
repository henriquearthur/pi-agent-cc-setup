# Changelog

## 1.0.0 — 2026-07-19

Initial public release.

- Claude Code–like TUI: header, statusline, compact bash/read traces
- Plan / execute workflow (`/plan`, `/execute`, `Ctrl+Alt+P`)
- `.claude/skills` and `.claude/rules` compatibility for trusted projects
- `claude-code` theme
- Prompt templates: commit, review, security-review, test
- Recommended settings + `APPEND_SYSTEM.md` preamble
- One-shot `scripts/install.sh` for fresh machines
